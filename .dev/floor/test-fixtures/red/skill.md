---
name: broken
role: skill
kind: pharn-owned
---

# Broken capability

Missing the required `version` field and has no evals, so the floor must flag it
as a blocking finding (RED) and exit non-zero.
