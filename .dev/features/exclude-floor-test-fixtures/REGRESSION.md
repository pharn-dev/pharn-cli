# REGRESSION — exclude-floor-test-fixtures

- base: `bb482a7e2ab6aefcf0fcd3223d1f0e30aace47a1` (`origin/main`) · machine report: `regression-report.json`

Build-only partition byte-identical to the plan's `## Files`; `scope` exit **0**, `escaped: []`.
Style gates skipped by the config-touch rule. Captures ran under `setopt SH_WORD_SPLIT`.

| gate       | base | head |
| ---------- | ---- | ---- |
| `tests`    | 0    | 0    |
| `validate` | 0    | 0    |

## Verdict (FLOOR — exit 0): `no-regressions`
