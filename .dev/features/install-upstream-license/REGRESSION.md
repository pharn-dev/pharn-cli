# REGRESSION — install-upstream-license

- base: `a858d0fa3833ad8b0ad9bd0e602813d1e1d5e06c` (`origin/main`) · machine report: `regression-report.json`

Build-only partition byte-identical to the plan's `## Files`; `scope` exit **0**, `escaped: []`.
Style gates skipped by the config-touch rule; captures under `setopt SH_WORD_SPLIT`.

| gate       | base | head |
| ---------- | ---- | ---- |
| `tests`    | 0    | 0    |
| `validate` | 0    | 0    |

## Verdict (FLOOR — exit 0): `no-regressions`
